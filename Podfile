# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DriveLogger' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase/Crashlytics'
  pod 'Firebase/Analytics'
  pod 'Firebase/Performance'
  # Pods for DriveLogger

end

target 'DriveLoggerCore' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DriveLoggerCore

end

target 'DriveLoggerServicePackage' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Disk', '~> 0.6.4'
  # Pods for TimeDrivenExtension

end
